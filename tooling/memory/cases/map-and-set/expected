ideal 2
allocated 17
because seventeen blocks, and they were invisible until this case existed.
because
because A `Map` is a header plus three blocks -- keys, values, and the slot
because index -- and a `Set` is a header plus two, having no values. The blocks
because are `malloc`ed rather than taken from `nts_alloc`, deliberately: a table
because reallocates as it grows and the bump provider cannot give a block back.
because
because What that cost was a *count*. `nts_note_allocation` says "objects,
because arrays, strings and maps all come through it", and a map's header did
because while the three blocks holding its contents did not -- so this column
because read `2` for two containers of any size, and could not have read
because otherwise. The bytes were already tracked; only the count was missing.
because Counting them is paired with `nts_reclaimed` at both free sites, or
because `nts_live_count` would read every grown table as a leak.
because
because Seventeen, exactly: growth doubles from a floor of eight, so seventeen
because entries means capacities 8, 16 and 32 -- three rehashes each.
because
because   Map   1 header + 3 rehashes x 3 blocks = 10
because   Set   1 header + 3 rehashes x 2 blocks =  7
because
because Fewer is possible only by being told the size in advance, and nothing
because tells it: `new Map()` says nothing about what goes in.
because
because Operations: two. The `Map` and the `Set` are each released once, where
because `work` returns and the last thing holding them lets go. Nothing else in
because this function touches a count.
because
because Not the keys and not the values, because they are numbers. A number
because erased into an `NtsValue` carries a number tag, and `nts_value_retain`
because reads that tag and returns without reaching `nts_retain` -- an emitted
because call, and not an operation this column counts.
because
because Not the fifty-one `set` and `add` calls either, and that is the whole
because of the difference from the naive column. `m.set(k, v)` evaluates to `m`
because so that calls chain, and a call's result used to be owned whatever it
because was: the callee retained on the way out and the caller released what it
because had thrown away. Fifty-one calls at two operations each is a hundred
because and two, and with the two above it is the hundred and four the naive
because column still reports. `own::RUNTIME_HANDS_BACK` names these functions
because now, so the result is recognized as the argument it already is.
because
because This half of the argument used to read "described rather than derived,
because and that is the weaker half". It derives exactly now, which is how the
because hundred and two were found: a number nobody could justify is a number
because worth looking at.

ideal 0
allocated 0
because seventeen boxes, one reference each to give back. The closures cost
because nothing: nothing lets one escape the iteration that built it, so each
because lives in the frame and is never an object at all.
because
because Written as thirty four first -- seventeen boxes and seventeen heap
because closures -- and the suite rejected it for being *below* what the
because compiler already does. It was the argument that was wrong, not the
because measurement, which is the whole reason the third column is a sentence
because and not a number.
because
because No retain is justified either. `box` is named once more after it is
because built, by the capture, and never again, so the frame's reference moves
because into the closure rather than being copied.
because
because Allocations: nothing. The box and the closure both die with the
because iteration that made them, and neither is named outside it, so a frame
because slot holds each in turn.
because
because Operations: none, and that follows from the allocation floor rather than
because from a separate argument. Nothing here reaches the allocator, and a
because frame object has no count to change: its storage ends with the frame
because whatever points at it, and a reference *to* one is a release the
because compiler can see is a no-op. An allocation floor of zero forces an
because operation floor of zero, and `run.sh` now refuses an `expected` that
because says otherwise.

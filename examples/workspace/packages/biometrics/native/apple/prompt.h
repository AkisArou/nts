/* LocalAuthentication (`LAContext`) is Objective-C; this is the same C-shim gap
   `notifications/native/apple/scheduler.h` records. */
#ifndef BIOMETRICS_PROMPT_H
#define BIOMETRICS_PROMPT_H
int authenticate(const char *reason);
#endif

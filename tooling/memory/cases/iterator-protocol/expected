ideal 0
allocated 0
because nothing in this walk leaves `work`, and that turns out to include the
because result object the protocol builds on every call.
because
because I argued eleven and eleven first, and the argument was wrong in a way
because worth keeping. `work(1)` walks from 9, so `next()` is called ten times
because -- nine that yield and one that reports there are none left -- and each
because returns a fresh `{ value, done }`. Add the `Steps` iterator and that is
because eleven objects, which is what the source says and not what runs.
because
because The wrong step was "born in a loop, so each needs its own storage".
because That is true in `store-elsewhere`, where thirty-three disks are threaded
because into a list and every one of them is live at the end. It is false here:
because each result dies before the next is made, so **one frame slot serves
because all ten**. Simultaneously live is the condition, not repeatedly made.
because
because Allocations: none. `next()` inlines, the result never escapes, and the
because slot is initialised inside the loop and reused. The `Counted` and the
because `Steps` are frame objects for the ordinary reason -- neither is
because returned, stored or passed anywhere that outlives the call.
because
because Operations: none. A frame object has no count, so there is nothing to
because retain when the loop reads a result and nothing to release when it
because drops one. The eleven releases I argued for were eleven objects that do
because not exist.
because
because What this pins is a strong claim rather than a small one: **iterating a
because user type through its own iterator allocates nothing**. The four
because built-in walks avoid the protocol -- an array is a counted loop, a `Map`
because is two reads and no pair -- and the reason given has always been that
because the protocol would allocate. On this shape it does not, and if this ever
because reads above zero the escape analysis stopped seeing through either the
because inlined `next()` or the returned result.

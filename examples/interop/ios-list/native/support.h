// The fixture's output: a line to stdout, flushed, since UIKit's process is
// ended by `exit` rather than by returning from `main`.
#ifndef NTS_IOS_LIST_SUPPORT_H
#define NTS_IOS_LIST_SUPPORT_H

void report(const char *line);

#endif

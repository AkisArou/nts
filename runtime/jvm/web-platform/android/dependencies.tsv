# The OkHttp production provider's third-party dependencies: pinned, hashed,
# licensed, and recorded here rather than resolved by a build file.
#
# A version range or a `+` would make the artifact that ships differ from the
# artifact that was reviewed, which is the whole of a supply-chain problem in
# one line. These are exact versions and exact SHA-256 digests of the exact
# jars, and the test that uses them verifies the digest before putting a jar on
# a classpath -- so a substituted artifact fails the build rather than shipping.
#
# The digests were taken from the jars fetched from Maven Central, each of
# which also matched the SHA-1 published alongside it. Two independent digests
# of the same bytes, which is the most the registry offers.
#
# The zero-invokedynamic rule is NOT applied to these. It is a rule about
# NTS-authored Java, kept so the artifacts this repository builds stay legible
# and desugar predictably; a third-party jar is reviewed on its own terms and
# passes through D8 like any other dependency.
#
# `scope` is `runtime` for anything that ships inside the APK and `tool` for
# anything that only builds it. R8 is a tool: it rewrites the artifact and is
# not in it, so it is pinned for reproducibility rather than reviewed as a
# dependency -- but a build tool that can be swapped silently is a supply-chain
# problem of exactly the same shape, which is why it is in the same file.
#
# `license` is an SPDX identifier and not the whole of the obligation, so here
# is the rest of it, checked rather than assumed. The three `runtime` jars ship
# inside the APK -- `build.gradle.kts` declares them `implementation`, not
# `compileOnly` -- so whoever packages the app redistributes Apache-2.0 code and
# owes its recipients a copy of the licence.
#
# **None of the three carries a `META-INF/NOTICE` or `META-INF/LICENSE`**, which
# was checked by listing them rather than inferred from the license field:
#
#     unzip -Z1 okhttp-4.12.0.jar | grep -i META-INF
#
# So section 4(d) -- propagate the NOTICE -- has nothing to propagate, and the
# remaining duty is section 4(a), which is a packaging step in the consuming
# app rather than something this library can discharge. Recorded because "we
# looked and there is none" and "we did not look" are different statements and
# only one of them survives being asked.
#
# One thing that listing turned up and is worth knowing if you shrink: OkHttp
# ships its own R8 rules at `META-INF/proguard/okhttp3.pro`. They are applied
# automatically to an app that bundles the jar; they are *not* applied by
# `r8_keeps_the_ffi_surface_and_removes_the_rest`, which passes these jars as
# `--lib` and shrinks only NTS-owned Java.
#
# `repository` because they are not all in one place. R8 is published to
# Google's Maven and not to Central, and a fetcher that assumed one host would
# quietly fail over to "cannot reach the network" for an artifact that is
# simply somewhere else.
#
# group	artifact	version	sha256	license	scope	repository
com.squareup.okhttp3	okhttp	4.12.0	b1050081b14bb7a3a7e55a4d3ef01b5dcfabc453b4573a4fc019767191d5f4e0	Apache-2.0	runtime	https://repo1.maven.org/maven2
com.squareup.okio	okio-jvm	3.6.0	67543f0736fc422ae927ed0e504b98bc5e269fda0d3500579337cb713da28412	Apache-2.0	runtime	https://repo1.maven.org/maven2
org.jetbrains.kotlin	kotlin-stdlib	1.8.21	042a1cd1ac976cdcfe5eb63f1d8e0b0b892c9248e15a69c8cfba495d546ea52a	Apache-2.0	runtime	https://repo1.maven.org/maven2
com.android.tools	r8	8.13.23	e3cdcb003d9beca956209ad6b9e9df31f26b732bfaed9c7c8674e903ca9f3b81	BSD-3-Clause	tool	https://maven.google.com

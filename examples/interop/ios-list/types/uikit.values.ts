// Generated by `nts bind-objc` beside the binding of `objc:UIKit`. Do not edit: regenerate.
//
// Swift's `async` imports: each method taking a completion handler, as a
// function returning a promise the handler settles, which the binding's
// `@ntsCall` overload names.
import { NSError, UIApplication, UIView } from "objc:UIKit";
import { nts_pending_begin, nts_pending_end } from "c:pending";
import type { CEnum } from "c:types";
import type { TimeInterval, UInt } from "objc:types";

export function nts_async_UIView_animateKeyframes(labels: { withDuration: TimeInterval; delay: TimeInterval; options: CEnum<UIView.KeyframeAnimationOptions | 0, UInt>; animations: () => void }): Promise<boolean> {
  return new Promise((resolve) => {
    nts_pending_begin();
    UIView.animateKeyframes(labels, (value: boolean) => {
      nts_pending_end();
      resolve(value);
    });
  });
}

export function nts_async_UIApplication_setAlternateIconName(self: UIApplication, alternateIconName: string | null): Promise<void> {
  return new Promise((resolve, reject) => {
    nts_pending_begin();
    self.setAlternateIconName(alternateIconName, (error: NSError | null) => {
      nts_pending_end();
      if (error !== null) {
        reject(new Error(error.localizedDescription));
      } else {
        resolve();
      }
    });
  });
}

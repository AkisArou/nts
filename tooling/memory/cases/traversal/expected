ideal 33
because building the chain owes nothing: each `made` is stored into `tail.next`
because and dies there, which is a transfer rather than a retain, and `tail` is
because an alias the loop never outlives. The walk owes nothing either -- it
because holds no store and no call, so every load reads the slot.
because
because What is owed is the teardown. Thirty three links have to be given back,
because and each one's `next` is a reference some object still holds until it is
because released. One release per link is the floor for reference counting; the
because way past it is not to count better but to not allocate thirty three
because objects, which is a different pass.

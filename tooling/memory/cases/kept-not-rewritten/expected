ideal 0
allocated 0
because nothing in this function is a counted reference. `width` and `total`
because are numbers, and the one string the source mentions is never built.
because
because Allocations: nothing, and what nothing stands in for is the point.
because `"grown" + i` is a concatenation, which is a string that has to be
because built, inside a loop that runs `8 + n` times. It is never built,
because because the right operand of `||=` is evaluated only on the path that
because writes and `width` is positive whenever the loop runs. A desugaring
because through `width = width || ("grown" + i).length` allocates `8 + n` times
because and is otherwise indistinguishable: same answer, same shape, same
because emitted code for the arm that never runs.
because
because The test is a real one. `width` is `8 + n` rather than a literal, so
because the compiler cannot fold the truthiness and cannot delete the arm --
because which matters, because a case where both spellings fold to nothing
because would report zero for both and prove nothing about either.
because
because Operations: none, and here that is the uninteresting half. Nothing
because counted exists, so nothing can be retained or released, and the naive
because column is what the string would have cost had it been built.
because
because The store's own memory cost is deliberately not measured here. A
because logical assignment that keeps its value performs no write, so it
because releases nothing -- but pinning that needs a *counted* target, and a
because counted target the arm can write means the frame walk must release
because whatever the field holds whichever path ran. That case reports one
because operation against zero allocations, which this suite correctly refuses
because as a contradiction. See the record: the release the writing arm does
because emit is of a slot the branch has just proved null, and eliding it wants
because a fact this compiler does not have.

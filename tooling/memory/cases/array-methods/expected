ideal 2
allocated 6
because two containers, released once each, and nothing else counted.
because
because The elements are numbers. A number in an array of numbers is not a
because reference and never was, so nothing about filling `xs` or reading it
because touches a count. What is left is the two arrays themselves: `xs` and
because the result of `filter`, each released where `work` returns.
because
because Not the four predicates. `some`, `every` and `findIndex` are loops with
because a carried answer -- a boolean or an index -- and a scalar carried
because between iterations is a block parameter, which is a machine register and
because not a reference. Six calls here and none of them costs an operation.
because
because Not `filter`'s last step either. `nts_array_keep_first` hands back the
because array it was given, and `own::RUNTIME_HANDS_BACK` names it, so the
because caller borrows what it was already holding rather than taking a
because reference and giving it back.
because
because Allocations: six.
because
because   xs        1 header, then 4 blocks grown through
because   filter    1
because
because `xs` is made empty and filled by `push`, which doubles from a floor of
because four, so seventeen elements pass through capacities 4, 8, 16 and 32.
because That is the same four `array-of-objects` pays and the same four a
because known trip count would remove; the argument is written out there.
because
because `filter` is the one. It allocates a result as long as its input, fills
because it from the front, and shortens it -- so the elements live inline in the
because header block the way every sized array's do, and one allocation covers
because the whole method however many elements survive it. Growing the result
because with `push` is the other way to write it, and it is what a hand-written
because loop does; it would pay a block every time it doubled, which is up to
because four more for an input this size.

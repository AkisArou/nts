ideal 0
allocated 0
because nothing here reaches the allocator. The box does not escape the
because iteration that built it, so it lives in the frame, and a frame object
because owes nothing to anybody: its storage ends with the frame.
because
because The erased local is the same reference under a tag, not a second one.
because `Erase` packs a pointer beside a tag and `Unerase` reads it back out;
because neither allocates and neither copies, so all three names in this loop
because are one object.
because
because Written as seventeen first -- one release per box -- which is the third
because time this column has counted objects that never became objects. The
because sixty eight operations it took before were retains and releases on a
because frame object, every one of them returning on its first line after a
because call and a branch. Zero counting is the right answer here and the
because instrument had to say so twice before I wrote it down.
because
because If this case ever needs an operation, erasure is costing an ownership
because kind of its own and record 0024 is wrong about it.
because
because Allocations: nothing, and this one already reaches it -- the box is a
because frame object today, which is why all sixty eight of its counting
because operations were on something that never saw the allocator.

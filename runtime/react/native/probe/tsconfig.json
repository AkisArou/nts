{
  "//": "The native probe: this lane's runtime compiled whole by nts, with a typed test host (src/ReactFiberConfig.ts) and a deterministic scheduler host (src/SchedulerHost.ts). `nts check` compiles it and compares its exported functions with node.",
  "extends": "../../tsconfig.native.json",
  "include": [
    "src"
  ]
}

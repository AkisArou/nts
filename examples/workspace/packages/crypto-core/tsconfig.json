{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "emitDeclarationOnly": true,
    "outDir": "./.types",
    "rootDir": "./src",
    "types": [
      "@nts/platform-android-29",
      "@nts/platform-ios-17",
      "@nts/platform-macos-14",
      "@nts/platform-linux-gnu",
      "@nts/platform-windows"
    ]
  },
  "include": [
    "src"
  ],
  "//": "A composite project, so apps can reference it and tsc can build the graph incrementally.",
  "//types": "The surfaces this package's nts.config.ts claims to support. It named none, so `c:types` -- which the standard C prelude declares -- was unresolved while the config said six platforms were fine."
}

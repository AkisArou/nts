ideal 0
allocated 0
because nothing here reaches the allocator, and nothing here is counted.
because
because `String(x)` is the one string-producing helper whose output length is
because known before it runs. A double's shortest round-tripping decimal needs
because at most seventeen significant digits, and the widest shape around them
because is `-1.2345678901234567e-308`: twenty-four characters.
because `NTS_NUMBER_STRING_MAX` is forty, which is that with room to be wrong
because in, and `frame_capacity` gives the call exactly that many units.
because
because So the string goes in the frame, and a frame string has no count to
because change: its storage ends with the frame whatever points at it. The
because text does not escape the iteration -- not stored, not returned, not
because passed anywhere that could keep it -- so escape analysis has nothing to
because stop it.
because
because This is the case that separates `String(x)` from `toLowerCase`.
because `case-convert` is at seventeen allocations and argues they are all
because necessary, because the length of a converted string is the length of
because its input and no compile-time bound exists. Here one does, and the
because floor is therefore zero rather than seventeen. If this case ever needs
because an allocation, either the bound was lost or the escape analysis stopped
because seeing that the result dies where it is made.
because
because Any number above zero on either column is a string that did not have
because to exist.

[package]
name = "nts-codegen-jvm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nts-codegen-common.workspace = true
nts-core.workspace = true
nts-diagnostics.workspace = true
nts-jvm-emitter.workspace = true
nts-semantic-schema.workspace = true

rustc-hash.workspace = true

[dev-dependencies]
# The one test that compiles TypeScript rather than driving `nts.rt` from Java.
# The C lane's `execute.rs` carries exactly this pair for exactly this reason;
# the alternative was shelling out to a built `nts`, which makes a test depend
# on a binary the gate happens to have left in `target/`.
camino.workspace = true
nts-frontend-ts.workspace = true

[lints]
workspace = true

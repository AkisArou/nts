{
  "//": "react-gtk's host config on real GTK widgets. The GIR bindings under types/gir are generated by `nts build`. It extends the native programs' base, so the reconciler modules the host config imports resolve to their sources and fork points.",
  "extends": "../../tsconfig.native.json",
  "include": [
    "src",
    "types",
    "../../../native/libc.d.ts"
  ]
}

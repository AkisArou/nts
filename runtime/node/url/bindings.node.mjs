// The native half of `node:url`, for the node-side run only.
//
// Nothing of its own: the platform and the warning sink are shared, and the
// IDNA step is TypeScript.
import "../internal/bindings.node.mjs";

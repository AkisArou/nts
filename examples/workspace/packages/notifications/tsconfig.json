{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "emitDeclarationOnly": true,
    "outDir": "./.types",
    "rootDir": "./src",
    "types": [
      "@nts/platform-android-29",
      "@nts/platform-ios-17",
      "@nts/platform-macos-14",
      "@nts/platform-linux-gnu",
      "@nts/platform-windows"
    ]
  },
  "include": [
    "src",
    "types"
  ],
  "//": "A composite project, so tsc can build the graph incrementally.",
  "//include": "`types` is where `nts build` writes the bindings this package's own sources import. Without it the package cannot typecheck in isolation -- in an editor, or in `tsc -b` -- because `c:` modules have no declaration until one is generated."
}

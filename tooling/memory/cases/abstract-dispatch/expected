ideal 0
allocated 0
because nothing, and the abstract declaration is not why.
because
because Allocations: none. Thirty-four shapes are built and each dies with the
because iteration that made it. `measure` takes one and returns a number, so
because nothing it does can keep the object past the call -- and a parameter
because that does not escape is what lets the caller put the object in a frame
because slot instead of on the heap. `borrowed-call` is the same argument for
because the same shape.
because
because Operations: none, and by the rule `closure-capture` states -- an
because allocation floor of zero forces an operation floor of zero.
because
because What the abstract method contributes is zero on both columns, and it
because contributes zero because it does not exist at run time in any form a
because count could reach. `Shape#area` is emitted as a signature whose body is
because `__builtin_unreachable()`, and `Shape` is never instantiated, so that
because slot is never the one dispatch lands on. The declaration buys the
because caller a function-pointer type and costs nothing.
because
because So this case is here to hold that still true. A lowering that gave an
because abstract method a real body would show up here only if the body
because allocated; one that boxed the receiver to dispatch through a slot it
because could not type would show up as thirty-four allocations and nowhere
because else.

# No sibling substitution. The upstream suite uses `fs`, timers, and
# `process.stdin` only as fixtures around the readline instance under test;
# readline itself imports none of those public modules or their state.

ideal 0
allocated 0
because a boolean is a machine bool. It is not managed, so `own::counted`
because answers no for it, and there is no retain or release to emit whatever
because it is done with.
because
because The four places it is put are the four places a value gets charged.
because
because A field: `gate.open` and `gate.seen` are stores into an object, and a
because store into a slot has to give up what the slot was holding -- that is
because what `nulled-field` is about, and it costs seventeen operations there.
because It costs nothing here because the slot holds a bool, so
because `layout.reference_fields()` for `Gate` is empty and there is nothing to
because release. If a boolean were ever laid out as a reference, that release
because would appear on this row and so would the walk a frame object's fields
because get at the end of their live range.
because
because A parameter and a return: `both` takes two and hands one back. An
because unmanaged parameter is not an obligation -- there is nothing to hand
because over and nothing to give back -- so a call that would cost two retains
because and two releases on references costs nothing.
because
because An erased slot: `boolean | undefined` needs a tag, because a bool has
because no spare bit pattern to spend on an absence any more than a double
because does. `Erase` packs the bool beside the tag and `??` reads it back out.
because `nts_value_retain` inspects the tag and finds no reference, which is
because the same argument `erased-slot` makes for a reference that *is* one.
because
because Allocations: the `Gate` never leaves `work` -- not returned, not stored
because into anything that outlives it, not passed anywhere -- so escape
because analysis puts it in the frame, and nothing else here is an object at
because all. Zero.
because
because Any number above zero on either column is the machine paying for
because something it holds in a register.

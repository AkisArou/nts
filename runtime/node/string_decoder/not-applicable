test-string-decoder.js: mixed language non-goal (§13); calls the exported class as `StringDecoder.call(arbitraryReceiver)` and later invokes `write` through `instance.__proto__`, requiring callable class/prototype metaobjects and unbranded receiver dispatch; local/core-static.js retains construction, normalization, state accessors, streaming UTF-8/UTF-16/base64 decoding, typed-array views, reuse after end, and public validation

test-string-decoder.js: mixed language non-goal (§13) plus host-engine stress; calls the exported class as `StringDecoder.call(arbitraryReceiver)` and later invokes `write` through `instance.__proto__`, requiring callable class/prototype metaobjects and unbranded receiver dispatch, while one half-GiB allocation probes V8's host-specific maximum string length; local/core-static.js retains every other construction, normalization, state, split-sequence, malformed-input, typed-view, reuse, and validation assertion

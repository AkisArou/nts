# Mixed/private Node v24.20.0 agent fixtures whose supported portions remain
# covered by focused tests under test/.
test-http-agent-close.js: language non-goal (typescript.md section 13); injects failure by replacing an Agent instance method at runtime, while the typed subclass construction/error/close path is retained in local/agent-construction-static.js
test-http-agent-keepalive-delay.js: language non-goal (typescript.md section 13); replaces an Agent instance method and invokes the saved method through Function.call, while the typed subclass override and keepAlive connection options are retained in local/agent-construction-static.js
test-http-agent.js: mixed language non-goal (typescript.md section 13); its second phase replaces Agent.prototype.createConnection at runtime, while ordinary concurrent requests and the typed connection-failure seam remain covered by the applicable max-socket fixtures and local/agent-construction-static.js
test-http-agent-remove.js: mixed private-internal fixture; after emitting the supported agentRemove lifecycle event it asserts the dynamically attached socket._httpMessage property, which a fixed-layout NTS socket does not have; pool detachment is retained in local/agent-remove-static.js
test-http-agent-free-socket-data-guard.js: mixed private-internal fixture; requires the dynamically attached socket.parser property to equal null; poisoning protection, zero public data/readable listeners, socket destruction, pool removal, and clean replacement are retained in local/agent-free-socket-guard-static.js
test-http-agent-uninitialized-with-handle.js: private-engine fixture; replaces net.Socket._handle with a fabricated JavaScript object and relies on Node's private handle asyncReset contract, while arbitrary uninitialized sockets without private handles remain exercised by test-http-agent-uninitialized.js
test-http-agent-domain-reused-gc.js: private-engine and language non-goal (typescript.md section 13); constructs internal/js_stream_socket, mutates its private handle, discovers ReusedHandle through observable constructor.name, and asserts V8 domain/GC retention of that private resource
test-http-outgoing-properties.js: mixed language non-goal (typescript.md section 13); replaces an OutgoingMessage instance method at runtime, while writable state, buffered length, response socket high-water mark, and ClientRequest metadata are retained through a statically declared subclass in local/outgoing-properties-static.js
test-http-outgoing-buffer.js: language non-goal (typescript.md section 13); replaces an OutgoingMessage instance method at runtime, while pre-socket high-water-mark backpressure, outputSize, writableLength, and writableNeedDrain are retained through a statically declared subclass in local/outgoing-properties-static.js
test-http-outgoing-message-inheritance.js: mixed private-internal fixture; assigns Node's dynamically attached writable._httpMessage ownership field, while static OutgoingMessage subclassing, the connection alias, headerless body delivery, and ordered end sentinel are retained in local/outgoing-properties-static.js
test-http-outgoing-proto.js: mixed language non-goal (typescript.md section 13); inspects prototype methods and invokes them through Function.call on fabricated receivers, while own-instance validation, destruction, and write errors remain covered by the applicable outgoing destroy/type and header-validation fixtures
test-http-outgoing-renderHeaders.js: private metaobject fixture (typescript.md section 13); imports internal/http.kOutHeaders, writes private dynamic symbol/_header fields, and asks _renderHeaders to reconstruct that engine-owned property table
test-http-outgoing-writableFinished.js: mixed private-internal fixture; its failure cases inject a generic Duplex by manually emitting Server's internal connection event, while normal client/server writableFinished timing and typed failing-socket callback semantics are retained in local/outgoing-properties-static.js
test-http-client-abort3.js: mixed language non-goal (typescript.md section 13); its second case replaces Agent.prototype.createConnection at runtime, while both the request-option seam and a statically declared Agent override retain post-abort connection errors in local/client-abort-connection-static.js
test-http-client-spurious-aborted.js: mixed private-engine and language non-goal (typescript.md section 13); rewrites res.socket._handle.close at runtime and assigns private readable state to fabricate a completed-yet-readable response, while ordinary premature-response aborted/error delivery is covered by test-http-client-aborted-event.js and a normally complete response remains covered throughout the client suite
test-http-client-timeout-agent.js: mixed language non-goal (typescript.md section 13); adds an undeclared id field to every ClientRequest and reads it back through callback receivers and the private response.req back-reference, while concurrent Agent scheduling and request timeout delivery/once-only behavior remain covered by the applicable Agent limit and client timeout fixtures
test-http-client-timeout-on-connect.js: private-engine fixture; imports internal/timers.kTimeout and inspects the private Timeout._idleTimeout object before and after connect, while the public socket.timeout transition from the option value to the setTimeout override is covered by test-http-client-set-timeout.js
test-http-parser-timeout-reset.js: private-native fixture; constructs internalBinding(http_parser).HTTPParser, installs numeric symbol callback slots, and consumes a net.Socket private native _handle directly, with no public node:http operation under test
test-http-server-close-destroy-timeout.js: private-engine fixture; imports _http_server.kConnectionsCheckingInterval and inspects the private timer object's _destroyed bit, while public server close and timeout-checker shutdown are exercised by the applicable server timeout suite
test-http-server-connection-list-when-close.js: private-engine and language non-goal (typescript.md section 13); overwrites the dynamically attached req.socket.parser.free method to keep Node's private native parser connection list stale, while public graceful close, closeAllConnections, and closeIdleConnections behavior remains covered by their applicable server fixtures
test-http-parser-freed-before-upgrade.js: private-engine fixture; both callbacks assert Node's dynamically attached socket.parser field is exactly null, while parser detachment before user ownership is exercised publicly by the applicable upgrade handoff, binary-head, timeout, and stream-reconsume fixtures
test-http-parser-memory-retention.js: private-engine fixture; reads request.socket.parser, request.parser, native numeric callback slots, parser.onIncoming, and parser.joinDuplicateHeaders to inspect llhttp cleanup, while the typed parser pool clears all callbacks and parse state before retention and public parser reuse/close/upgrade paths remain applicable
test-http-upgrade-server.js: language non-goal (typescript.md section 13); constructs callable Server inheritance through two runtime Object.setPrototypeOf mutations, while listener/default-policy, raw head handoff, ordinary requests, and no-listener fallback are covered by applicable upgrade fixtures using static construction
test-http-connect.js: mixed private-engine fixture; asserts dynamically attached socket._httpMessage plus private ondata/onend callback slots and exact internal listener topology before exercising the tunnel, while public CONNECT request/response heads, Agent removal, full-duplex ownership, close lifecycle, and EOF behavior are covered by test-http-connect-req-res.js and test-http-eof-on-connect.js
test-http-sync-write-error-during-continue.js: mixed private-engine and language non-goal (typescript.md section 13); injects a host stream.duplexPair endpoint as an HTTP transport, nulls its private _writev slot, and replaces its _write method at runtime to force a synchronous native-parser reentrancy path, while public Expect/100-continue delivery, error forwarding, and keep-alive reuse remain covered by the applicable continue fixtures
test-http-response-cork.js: language non-goal (typescript.md section 13); replaces the accepted socket's write method at runtime and invokes the saved method through Function.call to observe cork batching, while public response cork/uncork completion and drained-socket reuse remain covered by the applicable outgoing-end-cork, response-drain-cork, and agent-reuse-drained-socket-only fixtures
test-http-destroyed-socket-write2.js: mixed private-internal fixture; its public null-response and RESET/ABORT/EPIPE contract passes, then it inspects Node's private ClientRequest.outputData queue; the public behavior is retained in local/destroyed-socket-write-static.js
test-http-req-close-robust-from-tampering.js: private metaobject fixture (typescript.md section 13); reads the socket's private _events table, enumerates its close handlers, and invokes each through Function.bind on a fabricated receiver, with no public node:http operation asserted
test-http-server-clear-timer.js: private-engine fixture; imports _http_server.kConnectionsCheckingInterval and asserts the private Timeout._destroyed bit after manually emitting listening, while public timeout-checker cleanup is covered by the applicable server timeout and connections-checking-leak fixtures
test-http-server-keepalive-end.js: mixed private-engine fixture; after observing the public request end event it reads the dynamically attached socket.parser and its private incoming field, while request end and parser release/collection are covered by test-http-server-consumed-timeout.js and test-http-server-keepalive-req-gc.js
test-http-nodelay.js: language non-goal (typescript.md section 13); replaces net.Socket.prototype.connect and invokes the saved method through Function.call, while Agent's typed noDelay default is retained through the InspectingAgent override in local/agent-construction-static.js

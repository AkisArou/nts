ideal 16
allocated 16
because sixteen links on the heap, one reference each, and the walk owes
because nothing.
because
because The head is not one of them. It is made once, before the loop, and
because never escapes `work` -- so it lives in the frame, and only the sixteen
because made *inside* the loop need dynamic storage, one frame slot being unable
because to hold a list.
because
because Written as seventeen first, counting every link the same way. The
because measurement came in below it, which is the seventh time this column has
because been wrong in my hand and right in the instrument -- and the first time
because being wrong cost a *pessimism* rather than an optimism.
because
because `traversal` and `local-anchor` still pay for their heads because a
because helper builds the list and hands it back, and a returned value does
because outlive the frame that made it. The difference is where the list is
because built, not what it is.
because
because Building it is all moves: `made` dies into `tail.next`, and `tail` is a
because borrow of a slot that was just written. Walking it holds no store and no
because call, so every step is a borrow of the link before it, back to a head
because the frame owns for the whole loop and both ways out.

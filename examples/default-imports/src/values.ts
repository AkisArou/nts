// A default export that is a value, and named exports beside it. `export
// default` is a *name* — the binding is called `default` in the module's
// namespace — so an import of it is an import of that name and nothing else is
// special about it.
const scale = 7;
export default scale;
export const offset = 3;

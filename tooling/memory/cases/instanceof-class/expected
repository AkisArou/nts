ideal 0
allocated 0
because `instanceof` reads one word out of a header and compares it against a
because set of descriptor pointers fixed when the program was built. It makes
because nothing, keeps nothing, and hands nothing on.
because
because Allocations: nothing. The `Shape` does not leave `work` -- it is asked
because for its class and dropped -- so escape analysis keeps it in the frame,
because and a frame object has no count to change.
because
because Operations: none, and the second half of that is the part worth
because pinning. The operand is *erased* on the way into the test, because a
because value that might not be an object has to say so, and an erase used to be
because counted unconditionally. It is what it wraps: a frame object with a
because number in it has nothing to give back, so neither does the wrapper.

ideal 0
allocated 0
because the `Tag` never leaves `work`, so it lives in the frame and has no
because count of its own. What it holds is a string literal, which is static
because data the runtime treats as immortal and which therefore has no count
because either.
because
because Allocations: nothing. Neither the object nor the string reaches the
because allocator.
because
because Operations: none, and this is the case that says so. The walk over a
because dying frame object's fields already skips a slot that only ever holds a
because null or another frame object -- but a slot holding a *literal* was not
because on that list, though the pass that decides whether a value needs
because counting at all had said `false` for a literal all along. Two lists
because answering one question, disagreeing about three of its cases; this is
because the one that costs, at a load, a call and a branch an iteration.

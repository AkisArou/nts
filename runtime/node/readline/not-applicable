test-readline-interface.js: mixed language non-goal (§13); the otherwise broad interface fixture calls the exported class without `new` and later requires the dynamic `util.promisify.custom` Symbol hook on `Interface.prototype.question`; local/interface-static.js retains constructible Interface, both createInterface call shapes, line splitting, callback questions, and the explicit readline/promises API
test-readline-csi.js: mixed private-internal language non-goal (§13); requires `internal/readline/utils.CSI` to be simultaneously callable as a template tag and carry observable function-object properties; local/cursor-static.js retains the public cursor movement, clearing, validation, callback, and null-stream behavior from the remainder of the fixture

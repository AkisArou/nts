{
  "//": "An editor opening a file here needs a project to resolve it against; without one it falls back to defaults and reports errors that the gate does not. This is that project. It is not part of `check.sh`, which type-checks the *source* through the sibling tsconfig; nothing here is emitted.",
  "extends": "../../../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "rootDir": "../../../..",
    "noEmit": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["./**/*.ts", "../*.ts", "../../../../runtime/web-platform/src/**/*.ts"]
}

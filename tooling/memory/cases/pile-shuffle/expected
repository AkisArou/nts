ideal 9
allocated 11
because nine operations to give back eleven objects, and the gap is who does the
because work. The ten disks are moved between piles a hundred and twenty eight
because times, so each is decremented without reaching zero and each becomes a
because cycle candidate -- and when the array is finally dropped, the release
because that reaches the top disk finds it buffered and leaves it for the
because collector. The chain goes back in one collection, and the harness reads
because its counters before collecting on purpose, so that work is not on this
because bill. `shared-tail` is the same accounting.
because
because Written as eleven first, one release per object, which is the second
because time this column has charged the program for what the collector does.
because
because The `Piles` object does not escape and lives in the frame; the array
because does not, because an array's elements are a block of its own and no
because frame slot holds one.
because
because The moving is free, and that is the whole point. A hundred and
because twenty-eight moves create nothing: `pop` reads `slots[at]` and
because overwrites that very slot on the next line, which is a *take* -- the
because reference moves out rather than being copied, and the store that
because overwrites owes nothing. `push` reads `here[at]` into `disk.next` and
because overwrites it with `disk`, which is the same shape one step along.
because
because What stops it today is the index. A slot is named by its container and a
because *constant* index, and `at` is a parameter -- so two reads of `slots[at]`
because with the same `at` are the same slot, SSA says so, and nothing says it.
because
because This is `awfy-towers` with the arithmetic taken out: 8191 moves of
because fourteen disks between three piles, allocating nothing after the first
because fourteen, and the worst row in the table at 6.36x C++.

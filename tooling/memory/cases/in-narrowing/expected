ideal 0
allocated 0
because nothing, on both columns, and the second follows from the first.
because
because Allocations: none. Seventeen shapes are built and each dies with the
because iteration that made it -- nothing stores one, returns one, or passes one
because to something that could keep it -- so a frame slot holds each in turn
because and none reaches the allocator. That the two arms are different types
because does not change it: a union of object types is still an object, and
because escape analysis asks where it goes rather than what it is.
because
because Operations: none, and by the rule `closure-capture` states rather than
because by a second argument -- an allocation floor of zero forces an operation
because floor of zero.
because
because Beyond that, `in` itself has nothing to count even where its operand is
because on the heap. The test reads the object's descriptor pointer and compares
because it against a set the compiler computed; it does not take a reference,
because does not hand one anywhere, and produces a `bool`. It is the same shape
because as `instanceof`, which `instanceof-class` already reads zero for.

ideal 18
allocated 17
because seventeen conversions, each producing a string that did not exist, and
because eighteen operations to account for them.
because
because Allocations first. The source is a literal, so it is `NTS_IMMORTAL` and
because reading it costs nothing. Its lowercase form is a different string --
because `Mixed Case Text Here` is not its own lowercase -- so every conversion
because allocates and none of the seventeen can be the same object. The `Box`
because never leaves `work` and is a frame object, so it adds none. Seventeen.
because
because Operations: seventeen stores and one field walk. Each `box.text = ...`
because overwrites a slot holding the only reference to the previous
because conversion, so each store must give that reference back -- and the walk
because a frame object's reference fields get at the end of their live range
because gives back the last one, which no store overwrites.
because
because Written as seventeen first, on the reasoning that the first store
because overwrites `""` and a literal is immortal, so that one is free. It is
because free at *run time* and it is not free here: what this suite counts is
because operations **emitted**, not objects touched, and a release of an
because immortal is still a call and a branch that ran. The instrument said 18
because and the argument was wrong by exactly the one I had talked myself out
because of.
because
because That release is elidable in principle -- the field's value at the first
because store is a literal the constructor put there -- but only by tracking a
because field's initial value through the constructor and into the loop, which
because nothing here does. It is one operation, named rather than assumed away.
because
because Not a frame allocation, and that is why the result is stored rather
because than dropped. A conversion whose result dies in the iteration that made
because it could live in the frame -- but only if the output length were known
because before the call, and `toLowerCase` of an arbitrary string has no
because compile-time bound. Writing the case that way would have made the floor
because an argument about what this compiler does rather than about what the
because program needs.

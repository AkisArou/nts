ideal 1
allocated 6
because the same six the accumulator costs in `string-append`, and nothing for
because the seventeen one-unit strings on the right.
because
because `String.fromCharCode` yields exactly one code unit, whatever it is given:
because the argument is truncated to sixteen bits. That is the easiest bound in
because the language, and a bound plus "does not outlive the frame" is what
because frame storage asks for -- the same pair that keeps a tokenizer's
because substrings out of the heap.
because
because The second half was the harder one. `escape` escapes every argument of
because every external call, because a body it cannot see could do anything with
because what it is handed -- and `nts_str_append` is one it *can* see, in
because `runtime/c`, where what it does with its right-hand argument is read it.
because Without that the temporary escapes into the call and no bound helps.
because
because One operation: the accumulator, released once where it dies. The
because temporary is immortal frame storage, so the release the counting pass
because emits for it returns on its first line.

ideal 33
allocated 33
because thirty three disks are born, and each one reference has to be given
because back. The table costs nothing: it does not escape, it lives in the
because frame, and it is marked immortal.
because
because Nothing here needs a retain. Every edge this program builds is a
because *move*: `made` dies into `table.top`, and the old `table.top` is read
because only to be written into `made.next` and then immediately overwritten,
because so the reference can be handed from the one field to the other without
because ever being duplicated. The walk at the end holds no store and no call.
because
because What must not happen is the read of `table.top` paying for a borrow
because because the loop also writes somewhere. Nothing the loop stores to could
because name the slot the read came from -- a different layout, a different
because field -- but the effects summary is one boolean per function, so
because "stores" ends the borrow. That is the whole distance between 33 and what
because this case reports, and it is the same boolean that keeps `awfy-towers`
because at 6.9x.
because
because Allocations: thirty three disks. The table is already a frame object;
because the list is not, for the usual reason.

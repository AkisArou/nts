ideal 18
allocated 22
because seventeen cells and the array that holds them, one reference each.
because
because Zero retains. `push` is handed a cell that is dying at the point it is
because handed over, so the reference moves into the element slot. The reading
because loop holds no store and no call between the load and the use, so the
because array cannot go away and the element cannot be replaced.
because
because The releases are the array's teardown: dropping it walks the elements
because once, which is one release per cell, and the array itself is the
because eighteenth.
because
because Allocations: seventeen cells, the array holding them, and the four
because blocks the array grew through on the way.
because
because The last four were invisible until `nts_array_reserve` started counting
because them. An array's header came through `nts_note_allocation` and the block
because holding its elements did not, so this column read the same number for an
because array of four and an array of four thousand -- the same hole
because `nts_map_rehash` had, found the same way and one column over from bytes
because that were already right.
because
because Four, exactly. `push` doubles from a floor of four, and the array is
because made empty, so seventeen elements pass through capacities 4, 8, 16 and
because 32: one allocation each, and a copy of everything already there.
because
because Eighteen is what this should be, and the difference is a thing the
because compiler does not do yet. The loop's trip count is `16 + n`, known
because before the loop starts and already computed by `hir::loops`; an array
because filled once per iteration by a loop of known length could be made at
because that length instead of grown to it, and its elements would live inline
because in the header block the way every sized array's do. Then the four go
because away and the seventeen cells and their array are all that is left.
because
because Recorded as a floor of 22 rather than an aspiration of 18 because 22 is
because what runs. The argument for 18 is above, and the ratchet moves when the
because compiler earns it.
